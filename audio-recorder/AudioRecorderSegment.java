// === SEGMENT: Audio snimanje i reprodukcija ===
// Kompletan kod je u README.md ovog foldera.
// Ovaj fajl je rezerva za brzo kopiranje.

import android.Manifest;
import android.content.pm.PackageManager;
import android.media.MediaPlayer;
import android.media.MediaRecorder;
import android.widget.Button;
import android.widget.Toast;
import androidx.core.app.ActivityCompat;
import androidx.core.content.ContextCompat;
import java.io.File;
import java.io.IOException;

// POLJA:
// private static final int REQ_AUDIO = 106;
// private Button btnSnimi, btnPusti;
// private MediaRecorder mediaRecorder;
// private MediaPlayer mediaPlayer;
// private boolean snima = false;
// private String audioPutanja;

// Vidi README.md za kompletne metode:
// pokreniSnimanje(), zapocniMediaRecorder(), zaustaviSnimanje(), pustiSnimak()
